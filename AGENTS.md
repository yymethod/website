# Repository Notes

- After committing and pushing changes for this site, always deploy production manually with `vercel deploy --prod --yes` from the repo root. Do not assume the GitHub push will trigger Vercel.
